#include <bits/stdc++.h>
#define MAX 500005

using namespace std;

int N;
int arr[MAX];
int ans[MAX];

void dc(int l, int r, vector<int> vt){
    if (l+1 == r) return;

}

int main(){
    #ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    #endif // ONLINE_JUDGE

    scanf("%d", &N);
    for (int i=0;i<N;i++){
        scanf("%d", &arr[i]);
    }



    return 0;
}
